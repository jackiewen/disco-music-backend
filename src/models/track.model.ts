import mongoose, { Schema, Document } from 'mongoose';
import BaseModel, { IBaseModel } from '@models/base.model';

export interface ITrack extends Document {
    
}

const TrackSchema: Schema = new BaseModel({

});